name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Checkout repo
        uses: actions/checkout@v2
        with:
          ref: 'main'

      - name: Release
        run: |
          git config --global user.name 'Martin'
          git config --global user.email 'martin.braquet@users.noreply.github.com'
          chmod +x ./release.sh
          ./release.sh
